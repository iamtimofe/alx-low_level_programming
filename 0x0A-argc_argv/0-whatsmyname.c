#include "main.h"
#include <stdio.h>

/**
 * main - prints the program name
 * @argc: parameter count
 * @argv: array
 * Return: Returns 0
 */
int main(int argc, char *argv[])
{
	(void) argc;

	printf("%s\n", argv[0]);

	return (0);
}
