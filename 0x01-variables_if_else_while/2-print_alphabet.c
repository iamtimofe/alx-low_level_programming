#include <stdio.h>
/**
 * main - Prints alphabelt in lower case
 *
 * Returns: Returns 0 Always
 */
int main(void)
{
	int i;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
	}
	{
		putchar('\n');
	}
	return (0);
}
